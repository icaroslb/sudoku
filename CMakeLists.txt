cmake_minimum_required(VERSION 3.10.0)
project(sudoku VERSION 0.1.0 LANGUAGES C CXX)

add_subdirectory(data)
add_subdirectory(src)

add_executable(sudoku main.cpp)
target_link_libraries(sudoku src data)